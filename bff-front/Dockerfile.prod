# Dockerfile.prod
# Utiliser quand tu as déjà construit `dist/` (build hors-image, par ex. dans CI ou localement).
# Image minimale nginx qui sert les fichiers statiques.
FROM nginx:alpine
WORKDIR /usr/share/nginx/html
# Copier la config nginx si présente
COPY nginx/nginx.conf /etc/nginx/nginx.conf
# Copier les fichiers buildés
COPY dist/ /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]

