apiVersion: v1
kind: Service
metadata:
  name: bff-service
  namespace: paytogether
  labels:
    app: bff
spec:
  type: LoadBalancer
  selector:
    app: bff
  ports:
    - protocol: TCP
      port: 8090
      targetPort: 8080
