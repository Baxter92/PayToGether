apiVersion: v1
kind: Service
metadata:
  name: registry
  namespace: paytogether
spec:
  type: ClusterIP
  ports:
    - port: 5000
      protocol: TCP
      targetPort: 5000
  selector:
    app: registry

