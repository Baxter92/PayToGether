# Configuration principale Spring Boot
spring.application.name=paytogether-bff
app.frontend.base-url= ${APP_FRONTEND_BASE_URL:https://dev.dealtogether.ca}

# Configuration JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configuration Flyway (migrations SQL) - DESACTIVE
spring.flyway.enabled=false

# Configuration DataSource
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://${DB_HOST:31.97.132.132}:${DB_PORT:5432}/${DB_NAME:postgres}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:ZhKdrpgibRkzWodAw6rRABraPdXRcbpW}

keycloak.client-id=buytogetherapi

# Configuration JWT
jwt.secret=${JWT_SECRET:monSecretSuperSecurisePourPayTogether2024!}
jwt.expiration=${JWT_EXPIRATION:86400000}
jwt.auth.converter.resource-id=${keycloak.client-id}
jwt.auth.converter.principal-attribute=preferred_username
# Configuration des API externes
api.auth.url=${API_AUTH_URL:http://connect-service:8092}
api.auth.apiKey=${API_AUTH_KEY:votre-cle-api-auth}
api.auth.admin.user=${API_AUTH_ADMIN_USER:hanaadmin}
api.auth.admin.user-id=${API_AUTH_ADMIN_USERID:37dc153b-bc3f-439d-9a53-ae86f2082690}
api.auth.admin.password=${API_AUTH_ADMIN_PASSWORD:$2a$10$ZCfYq7kvNvLsuUKmzDWkjebC2QozV9L8whpF6n85BGwtBFABEuD62}
api.paiement.url=${API_PAIEMENT_URL:http://localhost:8082}
api.paiement.apiKey=${API_PAIEMENT_KEY:votre-cle-api-paiement}

# Configuration MinIO
minio.url=${MINIO_URL:https://registry.dealtogether.ca}
minio.access.name=${MINIO_ACCESS_NAME:ulrichAfric}
minio.access.secret=${MINIO_ACCESS_SECRET:ulrSms@Afric}
minio.bucket.name=${MINIO_BUCKET_NAME:africbucket}
minio.default.folder=/
minio.presigned.url.expiration=${MINIO_PRESIGNED_URL_EXPIRATION:900}

# Configuration Square Payment
square.access-token=${SQUARE_ACCESS_TOKEN:EAAAlzEGRN5icWUbx_jZpcwCStCKIyusp1RkkgAEbH822aEO2UWkGmSufogLHnpK}
square.environment=${SQUARE_ENVIRONMENT:PRODUCTION}
square.location-id=${SQUARE_LOCATION_ID:LCWBGZWCNNH4D}
square.application-id=${SQUARE_APPLICATION_ID:sq0idp-YAZ1sLg98xrLqqeKws35cg}

# configuration email
spring.mail.host=smtp.titan.email
spring.mail.port=587
spring.mail.username=contact@dealtogether.ca
spring.mail.password=DEAL@2026
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Configuration Elasticsearch
# En local : http://localhost:9200
# En Kubernetes : http://elasticsearch-service:9200 (meme namespace paytogether)
spring.elasticsearch.uris=${ELASTICSEARCH_URIS:http://elasticsearch-service:9200}
spring.elasticsearch.username=${ELASTICSEARCH_USERNAME:}
spring.elasticsearch.password=${ELASTICSEARCH_PASSWORD:}
spring.data.elasticsearch.repositories.enabled=true

# Configuration Actuator & Prometheus
# Exposer tous les endpoints actuator (health, metrics, prometheus, info, etc.)
management.endpoints.web.exposure.include=health,metrics,prometheus,info
management.endpoint.health.show-details=always

# Métriques Prometheus (Spring Boot 3.x)
# Note: management.endpoint.prometheus.enabled et management.metrics.export.prometheus.enabled
# sont dépréciés et activés par défaut avec micrometer-registry-prometheus

# Personnalisation des métriques HTTP
management.metrics.distribution.percentiles-histogram.http.server.requests=true

# Service Level Objectives pour les métriques HTTP (en Duration format)
management.observations.http.server.requests.name=http.server.requests

# Tags personnalisés pour les métriques
management.metrics.tags.application=${spring.application.name}
management.metrics.tags.environment=production

# Port pour les métriques (optionnel, par défaut utilise le même port que l'application)
# management.server.port=9090

